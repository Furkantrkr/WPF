<Window x:Class="WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="75"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <TextBox Name="txtEntry" FontSize="16" Height="35" />
        <Button Grid.Column="1" Name="btnAdd" Width="50" Height="35" Content="Add" 
                Click="btnAdd_Click" HorizontalAlignment="Left"/>
        <Button Grid.Column="1" Name="btnDelete" Width="50" Height="35" Content="Delete" 
                Click="btnDelete_Click"/>
        <Button Grid.Column="1" Name="btnClear" Width="50" Height="35" Content="Clear" 
                Click="btnClear_Click" HorizontalAlignment="Right"/>
        <ListView Name="lvEntries" Grid.Row="1" Grid.ColumnSpan="2" SelectionMode="Extended" ItemsSource="{Binding Entries}"/>
    </Grid>
</Window>
